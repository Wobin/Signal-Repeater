name: Release Signal Repeater

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: Version to upload (e.g. 1.0.0)
        required: true

jobs:
  package-and-upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0

      - name: Set version
        run: echo "VERSION=${{ github.event.release.tag_name || inputs.version }}" >> "$GITHUB_ENV"

      - name: Verify every cue decodes to audio
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg
          find audio -name '*.ogg' -print0 \
            | xargs -0 -P "$(nproc)" -I{} sh -c '
                bytes=$(ffmpeg -v error -i "$1" -f wav - 2>/dev/null | wc -c)
                [ "$bytes" -lt 1000 ] && printf "%s\n" "$1"
                exit 0
              ' _ {} > empty-oggs.txt
          total=$(find audio -name '*.ogg' | wc -l)
          if [ -s empty-oggs.txt ]; then
            echo "::error::$(wc -l < empty-oggs.txt) of $total ogg files decode to no audio"
            cat empty-oggs.txt
            exit 1
          fi
          echo "All $total ogg files contain audio."

      - name: Package (no git artefacts)
        run: |
          git archive --format=zip --prefix="Signal Repeater/" \
            -o "Signal-Repeater-${VERSION}.zip" HEAD

      - name: Verify zip contents
        run: |
          unzip -l "Signal-Repeater-${VERSION}.zip"
          if unzip -l "Signal-Repeater-${VERSION}.zip" | grep -E '\.git|\.github|tests/'; then
            echo "Dev artefacts found in zip" && exit 1
          fi
          unzip -l "Signal-Repeater-${VERSION}.zip" | grep -q 'Signal Repeater/Signal Repeater.mod' \
            || { echo "Missing Signal Repeater.mod" && exit 1; }
          unzip -l "Signal-Repeater-${VERSION}.zip" | grep -q 'audio/cues/poxburster_beep/' \
            || { echo "Missing cue audio" && exit 1; }

      - name: Upload build artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: Signal-Repeater-${{ env.VERSION }}
          path: Signal-Repeater-${{ env.VERSION }}.zip

      - name: Resolve Nexus file_id (v2 GraphQL groupId)
        if: ${{ !github.event.release.prerelease }}
        run: |
          FID=$(curl -sS -H "apikey: ${{ secrets.NEXUSMODS_APIKEY }}" -H "content-type: application/json" \
            -X POST "https://api.nexusmods.com/v2/graphql" \
            -d '{"query":"query{modFiles(modId:\"${{ vars.NEXUS_MOD_ID }}\",gameId:\"4943\"){primary category groupId}}"}' \
            | jq -r '([.data.modFiles[]|select(.primary==1)]+[.data.modFiles[]|select(.category=="MAIN")])[0].groupId')
          if [ -z "$FID" ] || [ "$FID" = "null" ]; then echo "no groupId" && exit 1; fi
          echo "NEXUS_FILE_ID=$FID" >> "$GITHUB_ENV"

      - name: Upload to Nexus Mods
        if: ${{ !github.event.release.prerelease }}
        uses: Nexus-Mods/upload-action@v1.0.0-beta.8
        with:
          api_key: ${{ secrets.NEXUSMODS_APIKEY }}
          file_id: ${{ env.NEXUS_FILE_ID }}
          filename: Signal-Repeater-${{ env.VERSION }}.zip
          version: ${{ env.VERSION }}
          display_name: Signal Repeater ${{ env.VERSION }}
          category: main
          description: ${{ github.event.release.body }}
